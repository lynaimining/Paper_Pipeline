# Paper Pipeline — Python 依赖
#
# GPU 环境 torch/transformers 版本差异较大，使用合理上界约束而非 == pin
# MinerU 需要独立 venv：
#   pip install magic-pdf[full] --extra-index-url https://wheels.myhloli.com

# API 客户端
openai>=1.0.0,<3          # DeepSeek 和 OpenAI 兼容接口

# 图像处理（Qwen VL 推理）
Pillow>=9.0.0,<13
torch>=2.0.0,<3            # GPU 推理必须
transformers>=4.45.0,<6    # Qwen2.5-VL 需要 4.45+
accelerate>=0.26.0,<2
qwen-vl-utils>=0.0.8       # Qwen VL 官方工具

# 数据处理
pandas>=1.5.0,<4

# 通用工具
requests>=2.28.0,<3
tqdm>=4.64.0,<5

# 测试（开发依赖）
pytest>=7.0.0,<9
pytest-asyncio>=0.21.0,<2
