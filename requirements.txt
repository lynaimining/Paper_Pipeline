# Paper Pipeline Final — Python 依赖

# API 客户端
openai>=1.0.0          # DeepSeek 和 OpenAI 兼容接口

# 图像处理（Qwen VL 推理）
Pillow>=9.0.0
torch>=2.0.0           # GPU 推理必须
transformers>=4.45.0   # Qwen2.5-VL 需要 4.45+
accelerate>=0.26.0
qwen-vl-utils>=0.0.8   # Qwen VL 官方工具

# 数据处理
pandas>=1.5.0

# 通用工具
requests>=2.28.0
tqdm>=4.64.0

# MinerU PDF 解析（在目标机器上单独安装，建议独立 venv）
# pip install magic-pdf[full] --extra-index-url https://wheels.myhloli.com
# 安装完后检查: magic-pdf --version
